# Веб-фреймворк
fastapi==0.115.6
uvicorn[standard]==0.34.0

# База данных
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1

# Валидация и настройки
pydantic[email]==2.10.4
pydantic-settings==2.7.1

# Аутентификация
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-jose[cryptography]==3.3.0
python-multipart==0.0.20

# AI провайдер — YandexGPT через httpx (Anthropic — опциональный, lazy-import).
# Чтобы использовать AI_PROVIDER=anthropic — установи отдельно: pip install anthropic
# HTTP клиент (используется YandexProvider, импорт из sdamgia)
httpx==0.28.1

# HTML-парсер для импорта банка заданий со sdamgia.ru (Решу ЕГЭ)
beautifulsoup4==4.12.3

# SVG → PNG конвертация для чертежей с Решу ЕГЭ (Expo Go не умеет SVG)
# Использует системную libcairo2 (ставится в Dockerfile)
cairosvg==2.7.1

# Redis — кэширование маркетплейса/предметов/тестов и rate-limit верификации email
redis==5.2.1

# Тестирование
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0

# Утилиты
python-dotenv==1.0.1
faker==33.1.0
